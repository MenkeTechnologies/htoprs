//! htoprs entry point.
//!
//! The TUI, process table, meters, and platform data collection are
//! not yet ported. This stub exists so the crate builds as a binary
//! while the port proceeds file-by-file from the htop C source.

fn main() {
    eprintln!("htoprs: port in progress — no runnable UI yet");
    std::process::exit(1);
}
