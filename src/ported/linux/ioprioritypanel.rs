//! Stub scaffold for `IOPriorityPanel.c` — NOT yet ported.
//!
//! Every `pub fn` below is a placeholder (`todo!()`) named after a real
//! htop C function so the port-purity gate accepts the module and the
//! port surface is laid out. Replace each stub with a faithful port of
//! the C body, updating the signature and the doc comment to `Port of
//! `IOPriorityPanel.c`:<line>.` as you go. `gen_port_report.py` counts these
//! `todo!()` bodies as *stubbed*, not *ported*, so scaffolding does not
//! inflate coverage.
#![allow(non_snake_case)]
#![allow(dead_code)]


/// TODO: port of `Panel* IOPriorityPanel_new(IOPriority currPrio` from `IOPriorityPanel.c:22`.
pub fn IOPriorityPanel_new() {
    todo!("port of IOPriorityPanel.c:22")
}

/// TODO: port of `IOPriority IOPriorityPanel_getIOPriority(Panel* this` from `IOPriorityPanel.c:56`.
pub fn IOPriorityPanel_getIOPriority() {
    todo!("port of IOPriorityPanel.c:56")
}
