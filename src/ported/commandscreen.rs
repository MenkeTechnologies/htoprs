//! Stub scaffold for `CommandScreen.c` — NOT yet ported.
//!
//! Every `pub fn` below is a placeholder (`todo!()`) named after a real
//! htop C function so the port-purity gate accepts the module and the
//! port surface is laid out. Replace each stub with a faithful port of
//! the C body, updating the signature and the doc comment to `Port of
//! `CommandScreen.c`:<line>.` as you go. `gen_port_report.py` counts these
//! `todo!()` bodies as *stubbed*, not *ported*, so scaffolding does not
//! inflate coverage.
#![allow(non_snake_case)]
#![allow(dead_code)]

/// TODO: port of `static void CommandScreen_scan(InfoScreen* this` from `CommandScreen.c:22`.
pub fn CommandScreen_scan() {
    todo!("port of CommandScreen.c:22")
}

/// TODO: port of `static void CommandScreen_draw(InfoScreen* this` from `CommandScreen.c:68`.
pub fn CommandScreen_draw() {
    todo!("port of CommandScreen.c:68")
}

/// TODO: port of `CommandScreen* CommandScreen_new(Process* process` from `CommandScreen.c:81`.
pub fn CommandScreen_new() {
    todo!("port of CommandScreen.c:81")
}

/// TODO: port of `void CommandScreen_delete(Object* this` from `CommandScreen.c:86`.
pub fn CommandScreen_delete() {
    todo!("port of CommandScreen.c:86")
}
